你帮我总结一下关于上述知识点，不要漏知识点，每个知识点都需要写到，有易错点或者需要注意点的话需要单独一行用红色字体标注提示，最后再附上一个相关函数列表，这个列表的要求：（把每种函数和参数，功能都列出来，列表最好够宽一点，每一行一个函数，每一行一个参数，多个参数多行）；
知识点需要色彩分明(除代码块部分外)：（#ee4c2e标识重点，#209fff一般表示一个东西、时间点（比如下文是对xxx描述那我会把xxx标蓝，国庆节10.1），#9221ff是举例（比如三原色：r，g，b那我会把r、g、b标#9221ff颜色），#32833a是备注、批注）；
在标题、关键点使用黑体，正文仍用默认字体以保持易读性，全文需统一风格
最后生成md格式文档文档，注意Markdown 代码语法比如使用python语言代码块为```python```(python是小写字符)使用C++语言则代码块语法为```cpp```（而不是C++）
