#!/usr/bin/env bash
set -euo pipefail

EXTENSION_UUID="curtain-wallpaper@local"

gnome-extensions disable "$EXTENSION_UUID" 2>/dev/null || true
sleep 0.3
gnome-extensions enable "$EXTENSION_UUID"
echo "已重载 $EXTENSION_UUID"
